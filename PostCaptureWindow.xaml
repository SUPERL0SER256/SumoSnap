<Window x:Class="SumoSnap.PostCaptureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SumoSnap Editor" Height="500" Width="950"
        WindowStartupLocation="CenterScreen"
        WindowStyle="ToolWindow"
        Background="#1E1E1E">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#222222"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <!-- Main content area: Image + Chat side by side -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition x:Name="ChatColumn" Width="0" />
            </Grid.ColumnDefinitions>
            
            <!-- Image Preview -->
            <Image x:Name="PreviewImage" Stretch="Uniform" Margin="10" Grid.Column="0"/>
            
            <!-- Chat Panel (hidden by default) -->
            <Border Grid.Column="1" x:Name="ChatPanel" Background="#252525" BorderBrush="#333" BorderThickness="1,0,0,0" Visibility="Collapsed">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Chat Header -->
                    <Border Grid.Row="0" BorderBrush="#333" BorderThickness="0,0,0,1" Padding="0,0,0,8" Margin="0,0,0,8">
                        <Grid>
                            <TextBlock Text="Ask Gemini" FontWeight="Bold" FontSize="14" VerticalAlignment="Center"/>
                            <Button Content="X" HorizontalAlignment="Right" FontSize="11" Padding="6,3" Click="CloseChatButton_Click"/>
                        </Grid>
                    </Border>
                    
                    <!-- Chat Messages -->
                    <ScrollViewer Grid.Row="1" x:Name="ChatScrollViewer" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="ChatMessages" Margin="0,0,0,8"/>
                    </ScrollViewer>
                    
                    <!-- Chat Input -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="ChatInput" Grid.Column="0" Background="#2A2A2A" Foreground="White" CaretBrush="White" BorderBrush="#444" Padding="8,6" FontSize="13" AcceptsReturn="False" KeyDown="ChatInput_KeyDown" VerticalContentAlignment="Center"/>
                        <Button Grid.Column="1" x:Name="SendButton" Content="Send" Margin="6,0,0,0" Click="SendButton_Click" Padding="12,6"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
        
        <!-- Bottom Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Copy" Margin="8" Click="CopyButton_Click"/>
            <Button Content="Save" Margin="8" Click="SaveButton_Click"/>
            <Button x:Name="UndoButton" Content="Undo" Margin="8" Click="UndoButton_Click" IsEnabled="False"/>
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="10,0"/>
            <Button Content="Enhance" Margin="8" Click="EnhanceButton_Click"/>
            <Button Content="Reframe" Margin="8" Click="ReframeButton_Click"/>
            <Button Content="Remove BG" Margin="8" Click="RemoveBgButton_Click"/>
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="10,0"/>
            <Button Content="Ask Gemini" Margin="8" Click="AskGeminiButton_Click"/>
            <Button Content="⚙" Margin="8" Click="SettingsButton_Click" FontSize="15"/>
        </StackPanel>
        
        <!-- Loading Overlay -->
        <Grid x:Name="LoadingOverlay" Visibility="Collapsed" Background="#B0000000">
            <TextBlock Text="Processing..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="18" FontWeight="Bold"/>
        </Grid>
    </Grid>
</Window>
